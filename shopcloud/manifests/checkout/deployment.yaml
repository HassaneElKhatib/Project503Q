# Checkout service deployment.
#
# IRSA role permissions for `checkout` ServiceAccount must include:
#   - sqs:SendMessage on the invoice queue ARN ONLY (not "*")
#   - secretsmanager:GetSecretValue on its own secrets
# Define this in C's terraform/modules/sqs-invoice with a tight IAM policy.
#
# DATABASE_URL must be the WRITER endpoint (NEVER the read replica) - checkout
# inserts orders. C's terraform/modules/rds exposes both writer_url and
# reader_url; we pick writer_url.
#
# KEDA scaling: if you install KEDA, add a ScaledObject pointing at the
# invoice queue and remove this HPA. KEDA scales on SQS depth, which is
# better for spikes than CPU. Until KEDA is in, CPU-based HPA is fine.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
  namespace: app
  labels:
    app: checkout
    tier: backend
spec:
  replicas: 1
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: checkout
  template:
    metadata:
      labels:
        app: checkout
        tier: backend
    spec:
      serviceAccountName: checkout
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: checkout
      containers:
        - name: checkout
          image: 712044128773.dkr.ecr.eu-central-1.amazonaws.com/shopcloud-prod-checkout:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: SERVICE_NAME
              value: checkout
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: shopcloud-env
                  key: environment
            - name: AWS_REGION
              valueFrom:
                configMapKeyRef:
                  name: shopcloud-env
                  key: aws_region
            - name: LOG_LEVEL
              value: INFO
            # Database - WRITER endpoint
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: shared-database
                  key: writer_url
            - name: DB_POOL_SIZE
              value: "5"
            - name: DB_MAX_OVERFLOW
              value: "5"
            # Same Redis instance cart uses
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: shared-redis
                  key: url
            # SQS invoice queue URL (set by C's Terraform)
            - name: INVOICE_QUEUE_URL
              valueFrom:
                secretKeyRef:
                  name: invoice-queue
                  key: url
            - name: SQS_MAX_ATTEMPTS
              value: "3"
            # CUSTOMER Cognito pool
            - name: COGNITO_USER_POOL_ID
              valueFrom:
                secretKeyRef:
                  name: cognito-customer
                  key: user_pool_id
            - name: COGNITO_APP_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: cognito-customer
                  key: app_client_id
            - name: COGNITO_REGION
              valueFrom:
                secretKeyRef:
                  name: cognito-customer
                  key: region
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 0
            periodSeconds: 2
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
      terminationGracePeriodSeconds: 30
