# Non-secret config shared by every service.
# Values are filled in by Terraform per cluster (dev cluster -> dev values).
apiVersion: v1
kind: ConfigMap
metadata:
  name: shopcloud-env
  namespace: app
data:
  environment: prod
  aws_region: eu-central-1
  public_origin: https://www.welovedassouki.store
