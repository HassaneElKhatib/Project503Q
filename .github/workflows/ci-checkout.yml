name: CI Checkout

on:
  push:
    paths:
      - "shopcloud/services/checkout/**"
      - "shopcloud/libs/**"
      - ".github/workflows/ci-checkout.yml"
      - ".github/workflows/reusable-build-deploy.yml"
  pull_request:
    paths:
      - "shopcloud/services/checkout/**"
      - "shopcloud/libs/**"
      - ".github/workflows/ci-checkout.yml"
      - ".github/workflows/reusable-build-deploy.yml"

permissions:
  contents: read
  id-token: write

jobs:
  checkout:
    uses: ./.github/workflows/reusable-build-deploy.yml
    with:
      service: checkout
      service_path: shopcloud/services/checkout
      deploy: true
      eks_cluster_name: shopcloud-dev-eks
      k8s_namespace: app-dev
    secrets:
      aws_role_arn: ${{ secrets.AWS_OIDC_ROLE_ARN_DEV }}
