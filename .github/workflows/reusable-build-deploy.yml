############################################
# Reusable build + push pipeline for any service
# Used by ci-<service>.yml workflows (default ECR: shopcloud-<service>).
# Prod manifests expect shopcloud-prod-<service>; either pass ecr_repository_base: shopcloud-prod
# with a prod OIDC role or build/push via shopcloud/scripts/push-prod-ecr.ps1 (:latest).
############################################
name: Reusable Build Deploy

on:
  workflow_call:
    inputs:
      service:
        description: "Service name (matches shopcloud/services/<service> and ECR repo suffix)."
        required: true
        type: string
      service_path:
        description: "Path to the service inside the repo. Defaults to shopcloud/services/<service>."
        required: false
        type: string
        default: ""
      python_version:
        required: false
        type: string
        default: "3.11"
      run_tests:
        required: false
        type: boolean
        default: true
      build_image:
        required: false
        type: boolean
        default: true
      aws_region:
        required: false
        type: string
        default: "eu-central-1"
      # ECR repo name = "{ecr_repository_base}-{service}" (e.g. shopcloud-auth vs shopcloud-prod-auth).
      ecr_repository_base:
        required: false
        type: string
        default: "shopcloud"
      deploy:
        description: "Whether to deploy to EKS after a successful build+push."
        required: false
        type: boolean
        default: false
      eks_cluster_name:
        description: "EKS cluster name (e.g. shopcloud-dev-eks). Required when deploy is true."
        required: false
        type: string
        default: ""
      k8s_namespace:
        description: "Kubernetes namespace to deploy into."
        required: false
        type: string
        default: "app"
    secrets:
      aws_role_arn:
        description: "OIDC role to assume for ECR push and EKS deploy (set per environment)."
        required: false

permissions:
  contents: read
  id-token: write

jobs:
  test:
    if: ${{ inputs.run_tests }}
    name: Lint & test ${{ inputs.service }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}

      - name: Install service + dev deps
        run: |
          set -euxo pipefail
          path="${{ inputs.service_path }}"
          if [ -z "$path" ]; then path="shopcloud/services/${{ inputs.service }}"; fi
          python -m pip install -U pip wheel
          python -m pip install -e "shopcloud[dev]"
          python -m pip install -e "${path}[dev]"

      - name: Ruff
        run: |
          path="${{ inputs.service_path }}"
          if [ -z "$path" ]; then path="shopcloud/services/${{ inputs.service }}"; fi
          libs_path="libs"
          if [ -d "shopcloud/libs" ]; then libs_path="shopcloud/libs"; fi
          ruff check "${path}" "${libs_path}"

      - name: Pytest
        run: |
          path="${{ inputs.service_path }}"
          if [ -z "$path" ]; then path="shopcloud/services/${{ inputs.service }}"; fi
          PYTHONPATH=$PWD pytest "${path}/tests" -q

  build:
    if: ${{ inputs.build_image }}
    needs: [test]
    name: Build & push ${{ inputs.service }}
    runs-on: ubuntu-latest
    env:
      AWS_ROLE_ARN: ${{ secrets.aws_role_arn }}
      AWS_REGION: ${{ inputs.aws_region }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        if: ${{ github.event_name != 'pull_request' && env.AWS_ROLE_ARN != '' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: ECR login
        if: ${{ github.event_name != 'pull_request' && env.AWS_ROLE_ARN != '' }}
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build image
        run: |
          set -euxo pipefail
          path="${{ inputs.service_path }}"
          if [ -z "$path" ]; then path="shopcloud/services/${{ inputs.service }}"; fi
          tag="${GITHUB_SHA::12}"

          docker_context="."
          if [[ "$path" == shopcloud/* ]]; then
            docker_context="shopcloud"
          fi

          docker build -f "${path}/Dockerfile" -t "${{ inputs.ecr_repository_base }}-${{ inputs.service }}:${tag}" "${docker_context}"

      - name: Push to ECR
        if: ${{ github.event_name != 'pull_request' && env.AWS_ROLE_ARN != '' }}
        run: |
          set -euxo pipefail
          tag="${GITHUB_SHA::12}"
          aws_account="$(aws sts get-caller-identity --query Account --output text)"
          base="${{ inputs.ecr_repository_base }}"
          repo="${aws_account}.dkr.ecr.${AWS_REGION}.amazonaws.com/${base}-${{ inputs.service }}"
          docker tag "${base}-${{ inputs.service }}:${tag}" "${repo}:${tag}"
          docker push "${repo}:${tag}"

  deploy:
    if: >-
      inputs.deploy &&
      inputs.eks_cluster_name != '' &&
      github.event_name != 'pull_request' &&
      github.ref == 'refs/heads/main'
    needs: [build]
    name: Deploy ${{ inputs.service }}
    runs-on: ubuntu-latest
    env:
      AWS_ROLE_ARN: ${{ secrets.aws_role_arn }}
      AWS_REGION: ${{ inputs.aws_region }}
    steps:
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update kubeconfig for EKS
        run: |
          aws eks update-kubeconfig \
            --name "${{ inputs.eks_cluster_name }}" \
            --region "${{ env.AWS_REGION }}"

      - name: Deploy to EKS
        run: |
          set -euxo pipefail
          tag="${GITHUB_SHA::12}"
          aws_account="$(aws sts get-caller-identity --query Account --output text)"
          image="${aws_account}.dkr.ecr.${AWS_REGION}.amazonaws.com/${{ inputs.ecr_repository_base }}-${{ inputs.service }}:${tag}"

          kubectl set image deployment/${{ inputs.service }} \
            ${{ inputs.service }}="${image}" \
            -n "${{ inputs.k8s_namespace }}"

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/${{ inputs.service }} \
            -n "${{ inputs.k8s_namespace }}" \
            --timeout=300s

      - name: Rollback on failure
        if: failure()
        run: |
          echo "::warning::Deployment failed — rolling back ${{ inputs.service }}"
          kubectl rollout undo deployment/${{ inputs.service }} \
            -n "${{ inputs.k8s_namespace }}"


